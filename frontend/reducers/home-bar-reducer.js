import merge from 'lodash/merge';

const _defaultState = {
};

const HomeBarReducer = function(state = _defaultState, action) {
  switch(action.type){
    default:
      return state;
  }
};

export default HomeBarReducer;
